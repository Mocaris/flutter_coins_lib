import 'dart:convert';


const mnemonic = 'prevent pact movie provide lens guess answer awful hawk doll dwarf ocean';

void main() async {
  // var wallet = EthWallet(mnemonic);
  // var wallet = BitCoinWallet(mnemonic);
  // wallet.generateAddress();
  // var address = await WalletHd.getAccountAddress(mnemonic);
  // print(jsonEncode(address));
  // print(bool);
}
